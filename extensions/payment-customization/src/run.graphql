query RunInput {
  cart {
    buyerIdentity {
      customer {
        hasTags(tags: ["pay by invoice"]) {
          hasTag
          tag
        }
      }
    }
  }
  paymentMethods {
    id
    name
  }
}
